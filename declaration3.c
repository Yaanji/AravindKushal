#include<stdio.h>

main()
{
	int a;
	printf("a=%d\n",a);//the opput will be a garbage value , a grabage value is that one which is empty in a memory location and assgined to a
	a = 10;
	printf("a is %d\n",a);
}
